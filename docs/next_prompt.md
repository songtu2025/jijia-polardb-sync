# Next Codex Prompt

请继续这个项目。

开始前请先阅读：

1. AGENTS.md
2. README.md
3. docs/progress.md
4. docs/decisions.md
5. config/api_config.example.yaml
6. config/jijia_api_catalog.generated.json

注意：

- 不要重建项目。
- 不要覆盖已有实现。
- 不要读取或输出 `.env` 中的真实敏感信息。
- 不要写入真实 API 凭证、数据库密码或 accessToken。
- 如果发现代码和文档状态不一致，先说明差异，再决定怎么处理。
- 完成本阶段后，请更新 `docs/progress.md`、`docs/decisions.md` 和 `docs/next_prompt.md`。
- 保持 KISS：先做最小可验证主流程，不要一次性做完整生产级同步。
- 你负责把控和审核项目结果，以真实命令、数据库状态和 Git diff 为准。
- 开发过程中继续调用 superpowers 插件。

当前阶段：

阶段 15D 已完成。`storage_inbound_detail` 在 12I named lock 已生效后继续完成 2000 窗口回填，最新批次 `sync_20260712_170335_445099` 成功补齐 2000 个缺失入库单详情；下一阶段 15E 建议继续 2000 窗口，并作为本组三轮第 2 轮。

当前事实：

- 当前 enabled API 有 45 个：`amazon_shop_page`、`org_manage_query`、`role_list`、`dictionary_query`、`rate_page`、`continent_country_tree`、`ship_transport_list`、`country_tree`、`category_page`、`brand_page`、`product_page`、`amazon_msku_page`、`parent_product_page`、`kb_product_page`、`fba_warehouse_page`、`store_location_page`、`multi_shop_query`、`platform_msku_page`、`crm_tags_page`、`inventory_team_query`、`fba_inventory_page`、`fba_inventory_v2_page`、`inventory_adjustments_page`、`product_inventory_page`、`storage_inbound_page`、`transfer_page`、`lot_no_page`、`procure_detail`、`storage_return_page`、`strategy_template_page`、`traffic_analysis_page`、`traffic_page`、`traffic_sku_page`、`shipment_data_page`、`storage_ledger_page`、`storage_ledger_detail_page`、`storage_ledger_month_page`、`inventory_receipts_page`、`purchase_sale_storage_fba_page`、`purchase_plan_page`、`product_detail`、`country_province_query`、`transfer_detail`、`lot_no_detail`、`base_currency_query`。
- 覆盖矩阵当前口径：公开文档 API 187 个，真实配置 API 51 个，enabled API 45 个，configured disabled 6 个。
- 当前 DB `api_config` 共 59 条；销售表现同一个文档接口被拆成 7 个 `api_code`。
- 销售表现 7 个配置为：`sales_analysis_seller_sku_page`、`sales_analysis_asin_page`、`sales_analysis_variation_asin_page`、`sales_analysis_sku_page`、`sales_analysis_spu_page`、`sales_analysis_country_page`、`sales_analysis_market_page`。
- 销售表现 7 个配置均为 `enabled=false`、`commit_per_page=true`、`write_batch_size=10`、`data_date_param=beginDate`、`rate_limit.sleep_seconds=20`、`retry.retries=1`。
- 销售表现最近单接口验证均成功：`seller_sku=2673` 行/14 请求，`asin=2651` 行/14 请求，`sku=2097` 行/11 请求，`spu=34` 行/1 请求，`variation_asin=113` 行/1 请求，`country=7` 行/1 请求，`market=24` 行/1 请求。
- 修正 `data_date_param` 后，`sales_analysis_spu_page` 批次 `sync_20260707_102027_648202` 成功写入 35 条，DB 核验新窗口 `data_date=2026-07-03`。
- 销售表现真实响应里 `dateLine` 字段存在但值为 JSON `null`；后续不要再依赖 `dateLine` 作为 `raw_api_data.data_date` 来源。
- `commit_per_page=true` 当前只用于 `--sync-api` 单接口验证路径；普通 enabled 同步路径未改变。
- `storage_inbound_detail` 当前配置为 `enabled=false`、`param_source.limit=2000`、`auto_advance=true`、`exclude_existing_target=true`，累计覆盖为 169506/174334 个上游去重 code，完成约 97.23%，剩余 4828。
- 12H 批次 `sync_20260710_193937_362020` 成功：2000 次请求、2000 条成功计数、失败 0；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`，`data_date` 覆盖 `2023-12-14` 到 `2024-10-21`。
- 12J 批次 `sync_20260710_215533_340234` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1853 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2024-10-21` 到 `2025-07-07`。
- 12K 批次 `sync_20260710_231247_400113` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1691 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-07-07` 到 `2026-01-29`。
- 12L 批次 `sync_20260710_234747_756768` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1882 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2022-09-20` 到 `2026-07-03`。
- 12M 批次 `sync_20260711_002724_893517` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1909 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2023-11-13` 到 `2026-07-03`。
- 12N 批次 `sync_20260711_010853_756197` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 2156 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2024-10-11` 到 `2026-06-23`。
- 12O 批次 `sync_20260711_015340_111753` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 2069 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2024-11-23` 到 `2024-12-03`。
- 12P 批次 `sync_20260711_023428_235025` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1730 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2024-12-03` 到 `2024-12-06`。
- 12Q 批次 `sync_20260711_031541_067013` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1600 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2024-12-06` 到 `2024-12-07`。
- 12R 批次 `sync_20260711_035846_323463` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1766 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2024-12-07` 到 `2024-12-10`。
- 12S 批次 `sync_20260711_043506_686038` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1356 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2024-12-10` 到 `2024-12-11`。
- 12T 批次 `sync_20260711_050800_503945` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1991 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2024-12-11` 到 `2024-12-17`。
- 12U 批次 `sync_20260711_055041_994564` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 2145 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 为 `2024-12-17`。
- 12V 批次 `sync_20260711_063413_245884` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 2181 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 为 `2024-12-17`。
- 12W 批次 `sync_20260711_071733_399568` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 2256 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2024-12-17` 到 `2024-12-21`。
- 12X 批次 `sync_20260711_080050_309908` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 2259 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2024-12-21` 到 `2025-01-13`。
- 12Y 批次 `sync_20260711_084422_720644` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1870 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-01-13` 到 `2025-01-25`。
- 12Z 批次 `sync_20260711_092336_648499` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1513 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-01-25` 到 `2025-02-25`。
- 13A 批次 `sync_20260711_095703_766673` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1753 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-02-25` 到 `2025-03-27`。
- 13B 首次后台启动后出现 named lock 被占用且 `information_schema.innodb_trx=1`，但无新 `sync_batch` 且日志为 0 字节；确认是本轮创建的 Python 进程后已停止，复核 named lock 释放且 `innodb_trx=0`。
- 13B 批次 `sync_20260711_104132_862768` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1665 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-03-27` 到 `2025-05-15`。
- 13C 批次 `sync_20260711_111607_098213` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1533 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-05-16` 到 `2025-06-19`。
- 13D 批次 `sync_20260711_114746_751848` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1595 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-03-08` 到 `2026-06-29`。
- 13E 批次 `sync_20260711_122050_679722` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1486 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-07-07` 到 `2025-08-04`。
- 13F 批次 `sync_20260711_125139_974360` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1729 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-08-04` 到 `2025-09-16`。
- 13G 批次 `sync_20260711_132609_580441` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1588 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-09-16` 到 `2025-11-04`。
- 13H 批次 `sync_20260711_135800_183626` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1644 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-11-04` 到 `2025-11-13`。
- 13I 批次 `sync_20260711_143112_733218` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1474 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-11-13` 到 `2025-11-22`。
- 13J 批次 `sync_20260711_150318_718092` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1567 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-11-22` 到 `2025-12-02`。
- 13K 批次 `sync_20260711_153428_727704` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1396 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-12-02` 到 `2025-12-03`。
- 13L 批次 `sync_20260711_160250_409869` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1944 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-12-03` 到 `2025-12-05`。
- 13M 批次 `sync_20260711_164043_135730` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1654 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-12-05` 到 `2025-12-06`。
- 13N 批次 `sync_20260711_171343_254803` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1792 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-12-06` 到 `2025-12-09`。
- 13O 批次 `sync_20260711_175024_981468` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1769 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-12-09` 到 `2025-12-16`。
- 13P 批次 `sync_20260711_182507_314696` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1604 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-12-16` 到 `2026-01-22`。
- 13P 同步命令出现 CLI false failure：终端输出通用失败且退出码为 1，但 DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示本批次实际成功。
- 13P 已修复 false failure 的最小可测边界：如果任务体已完成但 `RELEASE_LOCK` 抛出 `SQLAlchemyError`，`_sync_task_lock` 现在只记录 warning 并关闭连接，不再覆盖已完成任务的结果；新增回归测试 `test_release_failure_after_successful_task_does_not_fail_task`。
- 13Q 批次 `sync_20260711_190058_925775` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1497 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-09-04` 到 `2026-06-02`。
- 13Q 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；未复现 13P 的锁释放 false failure。
- 13R 批次 `sync_20260711_193306_928252` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1272 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-11-12` 到 `2025-11-25`。
- 13R 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；未复现 13P 的锁释放 false failure。
- 13S 批次 `sync_20260711_195939_221698` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1490 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-11-25` 到 `2025-12-03`。
- 13S 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；未复现 13P 的锁释放 false failure。
- 13T 第一次单接口执行返回通用失败；复核显示未生成新批次、覆盖仍为 95506/174334、失败日志为 0、named lock 空闲、外部 `information_schema.innodb_trx=0`，随后 `--test-token` 成功，因此按未落库的前置阶段瞬时失败处理。
- 13T 重跑批次 `sync_20260711_205210_613646` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1395 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-12-03` 到 `2025-12-05`。
- 13T 重跑同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功。
- 13U 批次 `sync_20260711_212350_303415` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1319 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-12-05` 到 `2025-12-10`。
- 13U 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功。
- 13V 批次 `sync_20260711_215114_204353` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1396 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-12-10` 到 `2025-12-16`。
- 13V 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功。
- 13W 批次 `sync_20260711_222000_690781` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1348 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-12-16` 到 `2025-12-29`。
- 13W 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功。
- 13X 批次 `sync_20260711_224737_012183` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1514 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-09-03` 到 `2026-05-08`。
- 13X 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功。
- 13Y 前置只读 `JSON_EXTRACT(raw_json)` 覆盖查询超时并留下线程 `5430744`；确认该线程仍在执行同一条只读 SQL 后已结束连接，随后复核外部 `information_schema.innodb_trx=0`、named lock 空闲。后续覆盖核验优先使用 `raw_api_data.source_primary_key` 口径，避免对大表做 JSON 长扫描。
- 13Y 批次 `sync_20260711_232150_630501` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1338 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-11-11` 到 `2025-11-29`。
- 13Y 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功。
- 13Z 批次 `sync_20260711_235035_970138` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1646 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-11-29` 到 `2025-12-04`。
- 13Z 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功。
- 14A 批次 `sync_20260712_002315_005152` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1668 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-12-04` 到 `2025-12-06`。
- 14A 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功。
- 14B 批次 `sync_20260712_005733_731330` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 2142 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-12-06` 到 `2025-12-10`。
- 14B 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 113506/174334，剩余 60828。
- 14C 批次 `sync_20260712_014014_564932` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1946 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-12-10` 到 `2025-12-17`。
- 14C 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 115506/174334，剩余 58828。
- 14D 批次 `sync_20260712_021800_702802` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1905 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-12-17` 到 `2026-02-25`。
- 14D 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 117506/174334，剩余 56828。
- 14E 批次 `sync_20260712_025458_566738` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1903 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-09-04` 到 `2026-07-05`。
- 14E 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 119506/174334，剩余 54828。
- 14F 批次 `sync_20260712_033311_412781` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1428 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-11-12` 到 `2025-11-21`。
- 14F 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 121506/174334，剩余 52828。
- 14G 批次 `sync_20260712_040244_654638` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1732 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-11-21` 到 `2025-11-29`。
- 14G 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 123506/174334，剩余 50828。
- 14H 批次 `sync_20260712_043724_015023` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1414 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-11-29` 到 `2025-12-03`。
- 14H 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 125506/174334，剩余 48828。
- 14I 批次 `sync_20260712_050745_681739` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 2018 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-12-03` 到 `2025-12-05`。
- 14I 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 127506/174334，剩余 46828。
- 14J 批次 `sync_20260712_054716_533385` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1990 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-12-05` 到 `2025-12-10`。
- 14J 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 129506/174334，剩余 44828。
- 14K 批次 `sync_20260712_062548_729441` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1858 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-12-10` 到 `2025-12-14`。
- 14K 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 131506/174334，剩余 42828。
- 14L 批次 `sync_20260712_070217_270119` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 2211 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-12-14` 到 `2025-12-30`。
- 14L 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 133506/174334，剩余 40828。
- 14M 批次 `sync_20260712_074605_594229` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1889 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-09-03` 到 `2026-07-01`。
- 14M 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 135506/174334，剩余 38828。
- 14N 批次 `sync_20260712_082337_762682` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1792 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-10-25` 到 `2025-11-12`。
- 14N 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 137506/174334，剩余 36828。
- 14O 批次 `sync_20260712_085846_427821` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1709 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-11-12` 到 `2025-11-14`。
- 14O 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 139506/174334，剩余 34828。
- 14P 批次 `sync_20260712_093229_427628` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1487 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-11-14` 到 `2025-11-21`。
- 14P 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 141506/174334，剩余 32828。
- 14Q 批次 `sync_20260712_100226_982262` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1653 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-11-21` 到 `2025-11-25`。
- 14Q 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 143506/174334，剩余 30828。
- 14R 批次 `sync_20260712_103531_227809` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1572 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-11-25` 到 `2025-12-01`。
- 14R 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 145506/174334，剩余 28828。
- 14S 批次 `sync_20260712_110921_589140` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1449 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-12-01` 到 `2025-12-04`。
- 14S 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 147506/174334，剩余 26828。
- 14T 批次 `sync_20260712_113859_437382` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1339 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-12-04` 到 `2025-12-05`。
- 14T 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 149506/174334，剩余 24828。
- 14U 批次 `sync_20260712_120648_798323` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1618 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 均为 `2025-12-05`。
- 14U 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 151506/174334，剩余 22828。
- 14V 批次 `sync_20260712_123918_371296` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1755 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-12-05` 到 `2025-12-06`。
- 14V 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 153506/174334，剩余 20828。
- 14W 批次 `sync_20260712_131413_719479` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1521 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-12-06` 到 `2025-12-09`。
- 14W 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 155506/174334，剩余 18828。
- 14X 批次 `sync_20260712_134502_879945` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1551 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-12-09` 到 `2025-12-10`。
- 14X 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 157506/174334，剩余 16828。
- 14Y 批次 `sync_20260712_141747_244175` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1454 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-12-10` 到 `2025-12-11`。
- 14Y 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 159506/174334，剩余 14828。
- 14Z 批次 `sync_20260712_144942_448994` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1627 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-12-11` 到 `2025-12-13`。
- 14Z 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 161506/174334，剩余 12828。
- 15A 批次 `sync_20260712_152306_276301` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1411 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-12-13` 到 `2025-12-16`。
- 15A 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 163506/174334，剩余 10828。
- 15B 批次 `sync_20260712_155325_112417` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1808 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-12-16` 到 `2025-12-17`。
- 15B 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 165506/174334，剩余 8828。
- 15C 批次 `sync_20260712_163010_583171` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1654 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 均为 `2025-12-17`。
- 15C 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 167506/174334，剩余 6828。
- 15D 批次 `sync_20260712_170335_445099` 成功：2000 次请求、2000 条成功计数、失败 0，耗时 1868 秒；本批次 raw 为 2000 条、2000 个 `source_primary_key`、2000 个不同主键、2000 个 `data_hash`、空主键 0，`data_date` 覆盖 `2025-12-17` 到 `2025-12-24`。
- 15D 同步命令正常返回 0，DB 批次、API 日志、raw、失败日志、named lock 和外部事务核验均显示成功；累计覆盖推进到 169506/174334，剩余 4828。
- 阶段 12F 曾遇到 `raw_api_data` upsert 锁等待超时，根因是后台启动尝试留下 MySQL Sleep 事务；如再遇到锁等待，应先查 `information_schema.processlist` 和 `information_schema.innodb_trx`。
- 本次只读复核发现 `information_schema.innodb_trx` 曾有 1 条 Sleep 事务，线程号 `5143219`，连接库为 `jijia_sync`；用户确认后已释放该线程，复查 `innodb_trx` 为空。
- 写库/真实同步入口已增加 MySQL named lock `jijia_polardb_sync_task`：覆盖 `--mock-sync`、`--test-api`、`--sync-api`、`--sync-enabled`、`--sync-api-configs`；dry-run、`--check-db`、`--test-token` 不加锁。
- 本轮验收：dry-run 45 个 enabled API、`compileall app tests`、93 个 unittest、`git diff --check` 和最终 DB 复核均通过；`git diff --check` 仅提示文档 LF/CRLF 替换警告。
- 15D 回填结束后 named lock 已释放，外部 `information_schema.innodb_trx=0`；本批次和该 API 累计 `failed_request_log` 均为 0。

建议目标：

- 优先继续 `storage_inbound_detail` 缺失扫描回填；下一轮建议继续 `limit=2000`，避免当前前台执行窗口再次被 5000 长任务拖住。
- 不要直接把 `storage_inbound_detail` 加入 enabled；它当前只覆盖 169506/174334。
- 不要直接把销售表现加入 enabled；先评估 7 个粒度每天按 20 秒页间隔运行的 cron 窗口。
- 继续只读关注其他 configured disabled API：`market_inventory_query`、`delivery_fee_query`、`inventory_event_page`、`inventory_age_page`。
- 不要直接启用超大接口：`inventory_event_page` 当前约 2669068 条，`inventory_age_page` 当前约 6597161 条且响应慢。
- 13Q-13S 三轮复盘已完成：三轮均成功，覆盖从 89506 推进到 95506/174334，净增 6000；三轮 CLI 均正常返回 0，未复现 13P 的锁释放 false failure。
- 13T-13V 三轮复盘已完成：三轮覆盖从 95506 推进到 101506/174334，净增 6000；除 13T 首次未落库前置阶段瞬时失败后重跑成功外，三轮最终批次均成功，且均为 2000 请求、2000 成功、0 失败。
- 按新目标模式，13W-13Y 三轮复盘已完成；13Z-14B 三轮复盘已完成；14C-14E 三轮复盘已完成；14F-14H 三轮复盘已完成；14I-14K 三轮复盘已完成；14L-14N 三轮复盘已完成；14O-14Q 三轮复盘已完成；14R-14T 三轮复盘已完成；14U-14W 三轮复盘已完成；14X-14Z 三轮复盘已完成；15A-15C 三轮复盘已完成，覆盖从 161506 推进到 167506/174334，净增 6000；15D 已作为下一组三轮第 1 轮完成，覆盖从 167506 推进到 169506/174334，净增 2000。下一轮 15E 将作为本组三轮第 2 轮。当前整体规划是继续 2000 窗口回填 `storage_inbound_detail`，暂不升到 5000，销售表现继续保持 disabled。

验收：

- 新接口、完整窗口或回填评估必须由公开文档、覆盖矩阵、真实请求、数据库只读查询或测试证明，不靠猜测字段。
- 如启用接口，必须证明 `api_config.enabled=1`、dry-run enabled 数量变化正确，并用真实同步批次证明成功。
- 如继续 `storage_inbound_detail`，必须证明 `exclude_existing_target=true` 生效、批次没有重复主键、失败日志为 0，并记录累计覆盖进度；当前覆盖基线是 169506/174334。
- 如继续推进销售表现，必须区分“7 个拆分 api_code”和“覆盖矩阵 1 个文档接口”的口径差异。
- `api_config` 与覆盖矩阵显示真实配置 API 或 enabled 数量符合本轮目标；当前基线是真实配置 API 51 个、enabled 45 个、configured disabled 6 个。
- `compileall` 和 `unittest discover` 通过。
- 继续保持 `.env`、token 缓存、日志和真实凭证不提交。
